[package]
name = "slipstream-dns"
version = "0.1.1"
edition = "2021"
description = "DNS codec for Slipstream's QUIC-over-DNS tunnel"
license = "Apache-2.0"
repository = "https://github.com/specflowdev/slipstream"
readme = "../../README.md"

[dependencies]
slipstream-core = { path = "../slipstream-core" }
tracing = { workspace = true }

[dev-dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
